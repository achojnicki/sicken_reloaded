## Installation

When installing on a virtual machine make sure that you allocate enough RAM memory(at least an 8GB of RAM) for the VM as installation may fail due to the building of the wxpython due to it's requirement for memory. Also make sure you're allocating enough CPUs to make sure the installation doesn't take forever. 